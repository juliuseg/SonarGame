using UnityEngine;

public class SetMatStructBuf : MonoBehaviour
{
    public Material material;
    public Mesh mesh;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        


    }

}
